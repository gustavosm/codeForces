#include <bits/stdc++.h>


using namespace std;
typedef long long ll;


ll v[100005];
ll v2[100005];
int main ()
{
	ios_base :: sync_with_stdio(0); cin.tie(0);
	
	int n;
	
	cin >> n;
	ll x;
	ll raiz;
	for (int i = 0 ; i < n ; ++i)
	{
		cin >> x;
		v[i] = x;
		v2[i] = x;
	}
	
		
}
