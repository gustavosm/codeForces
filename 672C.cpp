#include <bits/stdc++.h>

using namespace std;
#define EPS 1e-9
typedef long long ll;
typedef long double ld;

int n;
vector<int> pset;

void init()
{
	pset.assign(n, 0);
	for (int i = 0 ; i < n; ++i) pset[i] = i;
}
int main()
{
	
}
