#include <bits/stdc++.h>

using namespace std;
int main()
{
	int n, m, k;
	int x;
	
	vector<int> v;
	
	while(cin >> n >> m >> k)
	{
		for (int i = 0 ; i < n ; i++)
		{
			cin >> x;
			v.push_back(x);
		}
		
	}
}
